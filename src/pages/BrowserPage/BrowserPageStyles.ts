import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
	pageHolder: {
		display: 'flex',
		margin: 10,
	},
	foldersCard: {
		width: '30%',
		height: 'calc(100vh - 40px)',
		padding: 10,
		marginRight: '10px'
	}
});
